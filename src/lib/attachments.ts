/**
 * Предел на размер вложения.
 *
 * Должен совпадать с `MAX_ATTACHMENT_BYTES` в `backend/src/api/items.rs`:
 * сервер отказывает по своему числу, и если клиент считает иначе, человек
 * заполнит форму, приложит паспорт инструмента и получит отказ в самом
 * конце — вместо отказа сразу, на выборе файла.
 */
export const MAX_ATTACHMENT_BYTES = 8 * 1024 * 1024
